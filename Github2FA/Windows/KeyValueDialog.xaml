<syncfusion:ChromelessWindow x:Class="Github2FA.Windows.KeyValueDialog"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:local="clr-namespace:Github2FA.Windows"
                             xmlns:vm="clr-namespace:Github2FA.ViewModels"
                             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
                             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=MaterialDarkBlue}"
                             mc:Ignorable="d"
                             Title="Enter Key and Value"
                             Height="220"
                             Width="300"
                             WindowStartupLocation="CenterOwner"
                             ResizeMode="NoResize"
                             TitleBarBackground="#87CE3E"
                             TitleBarForeground="White"
                             ShowInTaskbar="False">

    <StackPanel Margin="24"
                FocusManager.FocusedElement="{Binding ElementName=KeyTextBox}">

        <TextBlock Text="Key"
                   Foreground="White"
                   FontWeight="Medium"
                   Margin="0,5" />
        <syncfusion:SfTextBoxExt x:Name="KeyTextBox"
                                 Watermark="Enter key"
                                 Focusable="True"
                                 Margin="0,0,0,12"
                                 Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="Value"
                   FontWeight="Medium"
Foreground="White"
                   Margin="0,5" />
        <syncfusion:SfTextBoxExt Watermark="Enter value"
                                 Margin="0,0,0,16"
                                 Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="72"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="Ok_Click" />
            <Button Content="Cancel"
                    Width="72"
                    IsCancel="True" />
        </StackPanel>
    </StackPanel>
</syncfusion:ChromelessWindow>
