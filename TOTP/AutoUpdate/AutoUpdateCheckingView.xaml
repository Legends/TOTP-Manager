<UserControl x:Class="TOTP.AutoUpdate.AutoUpdateCheckingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xaml="clr-namespace:TOTP.Xaml"
             mc:Ignorable="d">
    <Grid Width="280">
        <StackPanel>
            <Border Height="74"
                    Margin="0,0,0,14"
                    CornerRadius="12"
                    ClipToBounds="True">
                <Grid>
                    <Image Source="/Assets/backgrounds/background-main.png"
                           Stretch="UniformToFill"
                           Opacity="0.48" />
                    <Rectangle Fill="{StaticResource Brush.SurfaceOverlay}" />
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="14,0,14,0">
                        <Image Source="/Assets/Icons/app.ico"
                               Width="24"
                               Height="24"
                               Stretch="Uniform" />
                        <TextBlock Text="{xaml:Resx Key=ui_Updater_Check_Title}"
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   Style="{StaticResource TextStyle.SectionHeader}" />
                    </StackPanel>
                </Grid>
            </Border>
            <TextBlock Text="{xaml:Resx Key=ui_Updater_Check_Header}"
                       Style="{StaticResource TextStyle.PageTitle}" />
            <TextBlock Text="{xaml:Resx Key=ui_Updater_Check_Description}"
                       Margin="0,8,0,0"
                       Style="{StaticResource TextStyle.Subheader}"
                       TextWrapping="Wrap" />
            <ProgressBar Height="8"
                         Margin="0,16,0,0"
                         IsIndeterminate="True"
                         Foreground="{StaticResource Brush.AccentPrimary}"
                         Background="{StaticResource Brush.SurfaceProgressTrack}"
                         BorderThickness="0" />
        </StackPanel>
    </Grid>
</UserControl>
