<UserControl x:Class="TOTP.Views.UnlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:xaml="clr-namespace:TOTP.Xaml"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Width="260" MinHeight="165">
        <StackPanel>

            <!-- First-run password setup -->
            <StackPanel VerticalAlignment="Center" Margin="10" Visibility="{Binding IsConfigured, Converter={StaticResource InverseBoolToVisibilityConverter}}">

                <TextBlock Text="{xaml:Resx Key=ui_Setup_Authorization}"
                           Style="{StaticResource TextStyle.SectionHeader}"
                           Margin="0,0,0,12" />

                <TextBlock Text="{xaml:Resx Key=ui_FirstRun_MasterPasswordRequired}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8" />

                <ContentControl Content="{Binding CurrentGate}" />

            </StackPanel>

            <!-- Once configured, every app start show configured gate Hello/PWD -->
            <ContentControl Content="{Binding CurrentGate}"
                            Visibility="{Binding IsConfigured, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        </StackPanel>
    </Grid>
</UserControl>
