<UserControl x:Class="TOTP.Views.PasswordUnlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:props="clr-namespace:TOTP.AttachedProperties">

    <StackPanel>
        <PasswordBox Height="30"
                     props:PasswordBoxHelper.BindPassword="True"
                     props:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" />
        <PasswordBox Height="30"
                     props:PasswordBoxHelper.BindPassword="True"
                     props:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" />
        <Button Content="Unlock"
                Command="{Binding UnlockCommand}"
                Height="36"
                Margin="0,10,0,0"/>
    </StackPanel>

</UserControl>