<UserControl x:Class="TOTP.Views.Components.BusyOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xaml="clr-namespace:TOTP.Xaml">
    <Border Background="{StaticResource Brush.SurfaceBusyOverlay}"
            Opacity="1"
            Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
            Panel.ZIndex="9999">
        <Border IsHitTestVisible="True">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Width="280">
                <TextBlock Text="{xaml:Resx Key=ui_Loading}"
                           Style="{StaticResource TextStyle.DialogTitle}"
                           TextAlignment="Center"
                           Margin="0,0,0,12" />
                <ProgressBar IsIndeterminate="True"
                             Height="6" />
            </StackPanel>
        </Border>
    </Border>
</UserControl>
