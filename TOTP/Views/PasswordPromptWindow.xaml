<syncfusion:ChromelessWindow x:Class="TOTP.Views.PasswordPromptWindow" 
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:viewModels="clr-namespace:TOTP.ViewModels"
                             xmlns:views="clr-namespace:TOTP.Views"
                             xmlns:userControls="clr-namespace:TOTP.UserControls"
                             d:DataContext="{d:DesignInstance Type=viewModels:PasswordPromptViewModel}"
                             xmlns:local="clr-namespace:TOTP.Views"
                             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
                             mc:Ignorable="d"
                             Title="{Binding Title}" 
                             Width="400" SizeToContent="Height" 
                             WindowStartupLocation="CenterOwner"
                             ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,15" FontWeight="SemiBold"/>

            <PasswordBox x:Name="PasswordInput" Height="30" Margin="0,0,0,20" VerticalContentAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Abbrechen" IsCancel="True"   Width="80" Margin="0,0,10,0"/>
                <Button Content="OK" IsDefault="True"  Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</syncfusion:ChromelessWindow>
