<UserControl x:Class="TOTP.UserControls.RevealableSecretBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:props="clr-namespace:TOTP.AttachedProperties"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:converters="http://sharpvectors.codeplex.com/svgc/"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- One-column overlay layout (like your original) -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Hidden secret (PasswordBox) -->
        <PasswordBox Height="{Binding FieldHeight, ElementName=Root}"
                     Padding="{Binding TextPadding, ElementName=Root}"
                     VerticalContentAlignment="Center"
                     props:PasswordBoxHelper.BindPassword="True"
                     props:PasswordBoxHelper.BoundPassword="{Binding Secret, ElementName=Root,
                             Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Visibility="{Binding IsSecretVisible,
                                          ElementName=Root,
                                          Converter={StaticResource InverseBoolToVisibilityConverter}}" />

        <!-- Visible secret (TextBox) -->
        <TextBox Height="{Binding FieldHeight, ElementName=Root}"
                 Padding="{Binding TextPadding, ElementName=Root}"
                 VerticalContentAlignment="Center"
                 Text="{Binding Secret, ElementName=Root,
                                Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding IsSecretVisible,
                                      ElementName=Root,
                                      Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- Eye button (overlay right) -->
        <ToggleButton HorizontalAlignment="Right"
                      Width="{Binding ToggleButtonWidth, ElementName=Root}"
                      Height="{Binding FieldHeight, ElementName=Root}"
                      Margin="{Binding ToggleButtonMargin, ElementName=Root}"
                      IsChecked="{Binding IsSecretVisible, ElementName=Root, Mode=TwoWay}"
                      ToolTip="{Binding ToggleToolTip, ElementName=Root}"
                      Focusable="False"
                      Style="{Binding ToggleButtonStyle, ElementName=Root}">

            <Grid Width="{Binding IconSize, ElementName=Root}"
                  Height="{Binding IconSize, ElementName=Root}">

                <!-- visible -> closed icon (your original behavior) -->
                <svgc:SvgViewbox Source="{Binding EyeClosedIconSource, ElementName=Root}"
                                 Stretch="Uniform"
                                 Visibility="{Binding IsSecretVisible,
                                                      ElementName=Root,
                                                      Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!-- hidden -> open icon -->
                <converters:SvgViewbox Source="{Binding EyeOpenIconSource, ElementName=Root}"
                                       Stretch="Uniform"
                                       Visibility="{Binding IsSecretVisible,
                                                            ElementName=Root,
                                                            Converter={StaticResource InverseBoolToVisibilityConverter}}" />
            </Grid>
        </ToggleButton>
    </Grid>
</UserControl>
