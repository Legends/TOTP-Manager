<UserControl x:Class="TOTP.UserControls.RevealableSecretBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:props="clr-namespace:TOTP.AttachedProperties"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:converters="http://sharpvectors.codeplex.com/svgc/"
             x:Name="rsbRoot"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- One-column overlay layout (like your original) -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Hidden secret (PasswordBox) -->
        <PasswordBox x:Name="PartPasswordBox" Height="{Binding FieldHeight, ElementName=rsbRoot}"
                     Padding="{Binding TextPadding, ElementName=rsbRoot}"
                     VerticalContentAlignment="Center"
                     props:PasswordBoxHelper.BindPassword="True"
                     props:PasswordBoxHelper.BoundPassword="{Binding Secret, ElementName=rsbRoot,
                             Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Visibility="{Binding IsSecretVisible,
                                          ElementName=rsbRoot,
                                          Converter={StaticResource InverseBoolToVisibilityConverter}}" />

        <!-- Visible secret (TextBox) -->
        <TextBox x:Name="PartPasswordBoxVisible" Height="{Binding FieldHeight, ElementName=rsbRoot}"
                 Padding="{Binding TextPadding, ElementName=rsbRoot}"
                 VerticalContentAlignment="Center"
                 Text="{Binding Secret, ElementName=rsbRoot,
                                Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding IsSecretVisible,
                                      ElementName=rsbRoot,
                                      Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- Eye button (overlay right) -->
        <ToggleButton HorizontalAlignment="Right"
                      Width="{Binding ToggleButtonWidth, ElementName=rsbRoot}"
                      Height="{Binding FieldHeight, ElementName=rsbRoot}"
                      Margin="{Binding ToggleButtonMargin, ElementName=rsbRoot}"
                      IsChecked="{Binding IsSecretVisible, ElementName=rsbRoot, Mode=TwoWay}"
                      ToolTip="{Binding ToggleToolTip, ElementName=rsbRoot}"
                      Focusable="False"
                      Style="{Binding ToggleButtonStyle, ElementName=rsbRoot}">

            <Grid Width="{Binding IconSize, ElementName=rsbRoot}"
                  Height="{Binding IconSize, ElementName=rsbRoot}">

                <!-- visible -> closed icon (your original behavior) -->
                <svgc:SvgViewbox Source="{Binding EyeClosedIconSource, ElementName=rsbRoot}"
                                 Stretch="Uniform"
                                 Visibility="{Binding IsSecretVisible,
                                                      ElementName=rsbRoot,
                                                      Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!-- hidden -> open icon -->
                <converters:SvgViewbox Source="{Binding EyeOpenIconSource, ElementName=rsbRoot}"
                                       Stretch="Uniform"
                                       Visibility="{Binding IsSecretVisible,
                                                            ElementName=rsbRoot,
                                                            Converter={StaticResource InverseBoolToVisibilityConverter}}" />
            </Grid>
        </ToggleButton>
    </Grid>
</UserControl>
